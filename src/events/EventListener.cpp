#include <events/EventListener.h>

/*
template <class T>
EventListener<T>::EventListener(EventSystem& system)
	: _eventSystem {system}
{
	_eventSystem.registerListener<T>(this);
}*/

/*
template <class T>
EventListener<T>::~EventListener()
{
	_eventSystem.removeListener<T>(this);
}
*/
